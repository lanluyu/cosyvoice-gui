# 复制本文件为 .env 并填入真实值。
#
# 说明：本项目的库与 CLI（main.py）直接读取环境变量，未内置 .env 自动加载；
# 请用 shell 的 export/set 设置下列变量，或借助 python-dotenv / direnv 加载 .env。
# 桌面应用（run_gui.py）可直接在界面「API Key」「代理」框填写，无需环境变量。

# 阿里云百炼 API Key —— 获取：https://help.aliyun.com/zh/model-studio/get-api-key
# 注意：北京（中国内地）与新加坡（国际）两个地域的 Key 互不通用。
DASHSCOPE_API_KEY=sk-your-key-here

# 可选：HTTP 代理（GUI 下载合成音频或调用 LLM 需要走代理时填写）
# http_proxy=http://127.0.0.1:7890
# https_proxy=http://127.0.0.1:7890
